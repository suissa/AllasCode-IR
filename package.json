{
  "name": "@allascode/ir",
  "version": "0.1.0",
  "description": "AllasCode IR and AllasDSL reference implementation",
  "type": "module",
  "bin": {
    "allas-ir": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "check": "tsc -p tsconfig.json --noEmit && vitest run",
    "cli": "tsx src/cli.ts"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "commander": "^14.0.1",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=24"
  },
  "license": "MIT"
}
